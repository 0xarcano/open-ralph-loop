---
description: Start Open Ralph Loop for a Lisa PRD
---

Start Open Ralph Loop for this Lisa spec:

`$ARGUMENTS`

Invoke the `ralph_start` tool with:

- `spec`: `$ARGUMENTS`

If no argument is provided, invoke `ralph_start` with no `spec` so it can resolve the default PRD.

After the tool returns, follow its instructions exactly.
